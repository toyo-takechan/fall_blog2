<li class="main_div_li">
<%= image_tag user.gravatar_url, :size=>50 %>
<%=" "%><%=link_to user.name, user_path(user) %>
<% if current_user.admin? && !current_user?(user) %>
  |  <%= link_to "プロフィール削除", user_path(user),data: {
                                                turbo_method: :delete,
                                                turbo_confirm: "Are you sure?"}, class:"mini_button" %>
<% end %>
</li>
